# Lever-LM 进度与交接（2026-06-03）

## 已完成

### 数据与修复
- **VQA v2**：5000 anchor，2万池，0% −∞，InfoScore + teacher-forcing
- **SST-2 v2**：1920 anchor（6920−5000 上限），5000 池，0% −∞
- 切分：VQA 4500/500；SST-2 1720/200

### 模型 P3（顺序 `[3,9]` vs `[9,3]`）
- 自回归 PointerSelector + `test_order_aware.py` 全 PASS
- 池真负样本训练：VQA 排列对顺序准确率 **0.609 → 0.739**

### 训练 checkpoint
| 任务 | 目录 | 最佳 val |
|------|------|----------|
| VQA | `checkpoints/lever_lm_v2_poolneg/` | loss≈7.08 |
| SST-2 | `checkpoints/lever_lm_sst2_poolneg/` | loss≈5.25 |

### 多任务
- `lever_lm/tasks.py` + `generate_data.py --task {vqa,caption,sst2}`
- Scorer 支持无图（SST-2）

## 进行中（16:08 更新）

- **Caption Step1**：✅ 运行中（GPU ~91%）
  - 进度：**200 / 2000**（10%），池 2000 图已就绪
  - 日志：`data/gen_caption.log`，断点 `data/caption_train_beams_v2.json`
  - 预计剩余：**~2.5 小时**（~5s/anchor）

## SST-2 已完成（本轮新结果）

- 数据 1920 条 + 切分 1720/200
- 训练：`checkpoints/lever_lm_sst2_poolneg/epoch016-valloss5.2548.ckpt`
- **留出集评测**（200 条，明显优于 VQA 上的随机水平）：
  - select 落在 beam 并集：**0.625**（随机 ≈0.19）
  - select 集合匹配最佳 beam：**0.185**
  - Top-1 beam：**0.395**

## 待做

1. Caption 跑完 → 切 train/val → 训练 `lever_lm_caption_poolneg`
2. 三任务下游 ICL 评测（`icl_inference.py`）
3. 更新 `Lever-LM实验报告.docx` / 对比文档

## 关键命令

```bash
# 查 Caption 进度
tail -f data/gen_caption.log
python -c "import json; print(len(json.load(open('data/caption_train_beams_v2.json'))))"

# Caption 续跑（若中断）
PYTHONPATH=. conda run -n leverlm python generate_data.py --task caption --anchor_random \
  --pool_size 2000 --max_samples 2000 --K 32 --beam_size 3 --shot_num 2 \
  --reward_mode info --resume --pool_in data/caption_pool_v2.json \
  --output_file data/caption_train_beams_v2.json --save_every 200 --seed 42

# 训练（模板）
PYTHONPATH=. conda run -n leverlm python train.py \
  --train_file data/XXX_train.json --val_file data/XXX_val.json \
  --d_model 1536 --K 32 --max_beams 3 --loss_mode rce \
  --neg_weight 1.0 --pool_file data/XXX_pool.json --num_workers 0 --gpus 1 \
  --run_name lever_lm_XXX_poolneg
```

## 已知结论（写报告用）

- 组内 3 beam reward 极差中位 ≈0.013 → RCE 难学细粒度排序；**检索 > 细选**
- 顺序问题：架构 + 池负样本已解决（>50% 基线）
- `d_model=1536`，勿用 768
