# Base 全流程 — 无人值守运行说明（2026-06-11）

你回家后 pipeline 会继续跑，**SSH 断开不会停任务**。

---

## 当前在跑什么

| 组件 | 说明 |
|------|------|
| **Step1** | `generate_data.py` 续跑 beam 4400→5000 |
| **之后自动** | Step2 训练 Selector → Step3 ICL（`icl_basefull_*`） |
| **守护进程** | `scripts/base_pipeline_watchdog.sh` 每 5 分钟检查，崩溃则自动重启 |

---

## 进程与日志

```bash
# 是否在跑
ps aux | grep -E 'run_base_full|generate_data.*v2_base|base_pipeline_watchdog' | grep -v grep

# Step1 进度（满 5000 即 Step1 完成）
python3 -c "import json; print(len(json.load(open('data/vqav2_train_beams_v2_base.json'))))"

# 主日志
tail -f data/base_full_pipeline.log

# 守护进程日志
tail -f data/base_pipeline_watchdog.log
```

---

## 全部完成标志

- 出现文件：`data/base_full_pipeline.DONE`
- ICL 结果：`results/icl_basefull_20k_*_unified_n5000.json`（5 个）

---

## 手动操作（回家后）

```bash
cd ~/lizhiheng/Lever-LM

# 若意外全停，可手动拉起（会自动 --resume）
nohup bash scripts/run_base_full_pipeline.sh >> data/base_full_pipeline.log 2>&1 &

# 或启动守护（推荐）
setsid nohup bash scripts/base_pipeline_watchdog.sh >> data/base_pipeline_watchdog.log 2>&1 &
```

---

## 预计剩余时间（粗估）

| 阶段 | 耗时 |
|------|------|
| Step1 剩 ~600 anchor | 2–4 h |
| Step2 训练 | 1–2 h |
| Step3 ICL | 7–8 h |
| **合计** | **约 10–14 h** |

---

## 勿删文件

- `data/vqav2_train_beams_v2_base.json`（Step1 checkpoint）
- `results/icl_base_*`（半套对照，勿覆盖）

---

*2026-06-11 19:30 已启动 watchdog + Step1 续跑*
